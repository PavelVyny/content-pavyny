---
phase: 04-foundation-generation
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - web/src/app/page.tsx
  - web/src/components/generate-form.tsx
  - web/src/components/format-card.tsx
  - web/src/components/script-display.tsx
  - web/src/components/loading-state.tsx
autonomous: false
requirements: [GENR-01, GENR-02, GENR-03]

must_haves:
  truths:
    - "Pavlo can see 7 format cards and select one"
    - "Pavlo can type dev progress context into a textarea"
    - "Clicking Generate calls the server action and shows a loading state"
    - "After generation completes, the script displays as structured beats with visual+voiceover pairs"
    - "Hook variants, title options, and anti-slop score are visible in the generated output"
    - "Pavlo can click Re-generate to produce a new script with the same or modified input"
  artifacts:
    - path: "web/src/components/generate-form.tsx"
      provides: "Format selector + dev context input + generate button"
      contains: "GenerateForm"
    - path: "web/src/components/script-display.tsx"
      provides: "Read-only display of generated script beats, hooks, titles, score"
      contains: "ScriptDisplay"
    - path: "web/src/components/format-card.tsx"
      provides: "Individual selectable format card"
      contains: "FormatCard"
    - path: "web/src/components/loading-state.tsx"
      provides: "Loading indicator during generation"
      contains: "LoadingState"
    - path: "web/src/app/page.tsx"
      provides: "Home page wiring form, display, and server data"
      contains: "getFormatList"
  key_links:
    - from: "web/src/components/generate-form.tsx"
      to: "web/src/app/actions/generate.ts"
      via: "import generateNewScript"
      pattern: "generateNewScript"
    - from: "web/src/app/page.tsx"
      to: "web/src/lib/references.ts"
      via: "getFormatList() call in server component"
      pattern: "getFormatList"
    - from: "web/src/components/script-display.tsx"
      to: "web/src/lib/types.ts"
      via: "Script type for props"
      pattern: "import.*Script.*from"
---

<objective>
Build the generation form UI, script display component, and wire everything together on the home page.

Purpose: Complete the end-to-end generation flow -- Pavlo selects a format, enters dev context, clicks Generate, sees a loading state, and then views the structured script with beats, hooks, titles, and score.
Output: Working generation page with form, loading state, and script display.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-foundation-generation/04-CONTEXT.md
@.planning/phases/04-foundation-generation/04-RESEARCH.md
@.planning/phases/04-foundation-generation/04-01-SUMMARY.md
@.planning/phases/04-foundation-generation/04-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From web/src/lib/references.ts:
```typescript
export function getFormatList(): VideoFormat[];
// Returns 7 formats: The Bug, The Satisfaction, Before/After, The Decision, The Trick, The Fail, The Number
```

From web/src/lib/types.ts:
```typescript
export interface VideoFormat { name: string; slug: string; description: string; beatCount: number; }
export interface Script { id: number; title: string; format: string; status: string; hooks: HookVariant[] | null; titles: string[] | null; thumbnail: string | null; durationEstimate: string | null; antiSlopScore: AntiSlopScore | null; beats?: ScriptBeat[]; ... }
export interface HookVariant { variant: string; visual: string; voiceover: string; }
export interface ScriptBeat { id: number; order: number; visual: string; voiceover: string; duration: string | null; }
export interface AntiSlopScore { directness: number; rhythm: number; trust: number; authenticity: number; density: number; total: number; notes: string; }
```

<!-- From Plan 02 -->
From web/src/app/actions/generate.ts:
```typescript
export async function generateNewScript(formData: FormData): Promise<{ success?: boolean; scriptId?: number; error?: string }>;
export async function regenerateScript(scriptId: number, format: string, devContext: string): Promise<{ success?: boolean; scriptId?: number; error?: string }>;
```

From web/src/lib/db/schema.ts:
```typescript
export const scripts: SQLiteTable;  // scripts table
export const beats: SQLiteTable;    // beats table
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generation form with format selector and loading state</name>
  <files>web/src/components/generate-form.tsx, web/src/components/format-card.tsx, web/src/components/loading-state.tsx, web/src/app/page.tsx</files>
  <read_first>
    web/src/lib/types.ts (VideoFormat, Script types)
    web/src/lib/references.ts (getFormatList function)
    web/src/app/actions/generate.ts (generateNewScript signature)
    web/src/components/ui/button.tsx (shadcn component available)
    web/src/components/ui/card.tsx (shadcn component available)
    web/src/components/ui/textarea.tsx (shadcn component available)
    .planning/phases/04-foundation-generation/04-RESEARCH.md (Generation Form Component example, Pitfall 5: loading states)
  </read_first>
  <action>
    Per D-09: Use frontend-design skill for all UI work.
    Per D-04: Clean light theme, Notion-style.
    Per D-03: No streaming -- show loading state during generation.
    Per GENR-01: Format selector (7 formats), dev progress text input, generate button.

    1. Create `web/src/components/format-card.tsx` — "use client" component:
       - Props: `{ format: VideoFormat; selected: boolean; onSelect: () => void }`
       - Render a shadcn Card with:
         - Format name as CardTitle (text-sm font-medium)
         - Format description as CardDescription (text-xs text-muted-foreground)
         - Beat count shown as a subtle badge: "{beatCount} beats"
       - When selected: `border-primary bg-primary/5` ring style
       - When not selected: `hover:border-muted-foreground/30` transition
       - onClick calls onSelect
       - `cursor-pointer` on the card

    2. Create `web/src/components/loading-state.tsx` — "use client" component:
       - Props: none (self-contained)
       - Shows a centered card with:
         - Animated pulse/spinner indicator (use Tailwind animate-pulse on a div)
         - Text: "Generating your script..."
         - Subtext: "Claude is reading your brand voice, selecting the format, writing beats, and scoring for anti-slop. This takes 30-60 seconds."
         - Elapsed time counter: useState + useEffect with setInterval updating every second, display as "Elapsed: {seconds}s"
       - Clean Notion-style layout: centered, generous padding, muted colors

    3. Create `web/src/components/generate-form.tsx` — "use client" component:
       - Props: `{ formats: VideoFormat[]; onScriptGenerated: (scriptId: number) => void }`
       - State: `selectedFormat` (string | null), `isGenerating` (boolean), `error` (string | null)
       - Layout:
         - Section heading: "Choose a Format" (text-lg font-semibold)
         - Grid of FormatCard components: `grid grid-cols-2 lg:grid-cols-3 gap-3`
         - Below the grid: Textarea for devContext with name="devContext"
           - Placeholder: "What did you work on? Be specific: bugs fixed, features added, numbers, tools used..."
           - `min-h-[120px]` for comfortable writing
         - Generate button: full-width, disabled when no format selected or isGenerating
           - Text: "Generate Script" or "Generating..." when loading
       - On submit:
         - Set isGenerating = true, clear error
         - Create FormData, set format to selectedFormat
         - Call `generateNewScript(formData)`
         - On success: call `onScriptGenerated(result.scriptId)`
         - On error: set error state, show toast via sonner
         - Finally: set isGenerating = false
       - When isGenerating: show LoadingState component instead of the form
       - Show error as a red text below the button if present

    4. Update `web/src/app/page.tsx` — server component:
       - Import `getFormatList` from `@/lib/references`
       - Call `getFormatList()` to get the 7 formats
       - Import `getDb` and query for the most recently generated script (if any) to display below the form
       - Query: `db.select().from(scripts).orderBy(desc(scripts.createdAt)).limit(1)` and its beats
       - Render a client wrapper component (or use a client component for the interactive parts):
         - The GenerateForm with formats passed as props
         - If a script was just generated (check URL search params or state), show ScriptDisplay (Task 2)
       - Title section: "Generate Script" as h2

    NOTE: The page needs to handle the state transition from "form" to "loading" to "script display". Use a client wrapper component `web/src/app/page.tsx` can be a server component that passes data to a client `GenerationPage` component, OR the page itself can be a client component that fetches formats. Choose the simpler approach: make the page a server component that renders the form, and use `router.refresh()` + `revalidatePath` to show the generated script after completion.

    For the post-generation display: after `onScriptGenerated` fires, use `useRouter().refresh()` to trigger a server re-render that loads the new script. The server component queries the latest script and passes it to ScriptDisplay.
  </action>
  <verify>
    <automated>cd D:/projects/content-pavyny/web && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/components/generate-form.tsx` contains "use client" and imports `generateNewScript`
    - `web/src/components/generate-form.tsx` contains `selectedFormat` state and `isGenerating` state
    - `web/src/components/format-card.tsx` contains "use client" and renders a Card with format.name
    - `web/src/components/loading-state.tsx` contains elapsed time counter with `setInterval`
    - `web/src/app/page.tsx` calls `getFormatList()` and passes formats to GenerateForm
    - All 7 format names appear in the rendered output (The Bug, The Satisfaction, etc.)
    - Build passes with no errors
  </acceptance_criteria>
  <done>Generation form renders 7 format cards in a grid, accepts dev context text, shows loading state with elapsed timer during generation, calls server action on submit</done>
</task>

<task type="auto">
  <name>Task 2: Script display component with beats, hooks, titles, and score</name>
  <files>web/src/components/script-display.tsx</files>
  <read_first>
    web/src/lib/types.ts (Script, ScriptBeat, HookVariant, AntiSlopScore types)
    web/src/app/actions/generate.ts (regenerateScript function)
    web/src/components/ui/card.tsx (shadcn Card available)
    web/src/components/ui/badge.tsx (shadcn Badge available)
    web/src/components/ui/separator.tsx (shadcn Separator available)
    scripts/007-dead-world-to-living-forest.md (example of expected script output format — dual-track table)
  </read_first>
  <action>
    Per D-04: Clean light theme, Notion-style.
    Per D-09: Use frontend-design skill for UI work.
    Per GENR-02: Display dual-track beats, hook variants, titles, thumbnail concept, anti-slop score.
    Per GENR-03: Re-generate button for full script re-generation.

    Create `web/src/components/script-display.tsx` — "use client" component:

    Props: `{ script: Script & { beats: ScriptBeat[] }; onRegenerate?: () => void }`

    Layout sections (vertical stack with separators between):

    1. **Header section:**
       - Script title as h2 (text-xl font-semibold)
       - Format badge (e.g., "The Bug") using shadcn Badge
       - Duration estimate if available (text-sm text-muted-foreground)
       - Status badge (draft/ready/recorded) with color coding

    2. **Title Options section:**
       - Heading: "Title Options" (text-sm font-medium uppercase tracking-wide text-muted-foreground)
       - Numbered list of 3 title options (1., 2., 3.)
       - Currently selected title (titles[0]) highlighted with bg-primary/10

    3. **Hook Variants section:**
       - Heading: "Hook Variants"
       - For each hook variant (A, B, C):
         - A card with variant label as badge
         - Two-column layout: VISUAL (left, text-muted-foreground) | VOICEOVER (right)
         - Visual column: italic, describes what viewer sees
         - Voiceover column: the spoken text, slightly larger font

    4. **Script Beats section:**
       - Heading: "Script Beats"
       - For each beat (ordered by `order`):
         - A card/row with beat number badge (#1, #2, ...)
         - Two-column layout: VISUAL | VOICEOVER (same style as hooks)
         - If beat has duration, show it as a subtle tag
       - Clean spacing between beats (space-y-3)

    5. **Anti-Slop Score section:**
       - Heading: "Anti-Slop Score"
       - Overall score prominently: "{total}/50" with color coding:
         - 35+: green (text-green-600)
         - 25-34: yellow (text-yellow-600)
         - <25: red (text-red-600)
       - 5-dimension breakdown as a simple grid:
         | Directness | Rhythm | Trust | Authenticity | Density |
         | 8/10       | 7/10   | 8/10  | 7/10         | 8/10    |
       - Notes text below in text-sm text-muted-foreground

    6. **Thumbnail Concept section** (if present):
       - Heading: "Thumbnail Concept"
       - Description text in a card

    7. **Actions section:**
       - "Re-generate" button: calls onRegenerate callback
       - This triggers the parent to show the form again with pre-filled format/context for GENR-03

    Styling notes:
    - Two-column beat/hook layout: use CSS grid `grid-cols-[1fr_1fr]` or `grid-cols-2`
    - Column headers: small uppercase labels "VISUAL" and "VOICEOVER" above the first row
    - Generous padding (p-4 on cards, space-y-6 between sections)
    - Notion-style: clean borders, subtle shadows, plenty of whitespace
  </action>
  <verify>
    <automated>cd D:/projects/content-pavyny/web && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/components/script-display.tsx` contains "use client"
    - Contains "Hook Variants" section rendering hook.variant, hook.visual, hook.voiceover
    - Contains "Script Beats" section mapping over beats with visual + voiceover columns
    - Contains "Anti-Slop Score" section with total/50 display and 5 dimensions
    - Contains "Title Options" section listing 3 titles
    - Contains "Re-generate" button with onRegenerate callback
    - Contains VISUAL and VOICEOVER column labels
    - Build passes with no errors
  </acceptance_criteria>
  <done>Script display shows all generated data in clean Notion-style layout: hook variants, dual-track beats, title options, anti-slop score breakdown, thumbnail concept, and re-generate action</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify end-to-end generation flow</name>
  <files>web/src/app/page.tsx</files>
  <action>
    Human verification of the complete generation pipeline. No code changes needed -- this task validates that Plans 01, 02, and 03 Tasks 1-2 produce a working end-to-end flow.
    The executor should start the dev server (`cd web && npm run dev`) before presenting the verification steps to the user.
  </action>
  <what-built>
    Complete script generation pipeline: Next.js app with format selector, dev context input, Agent SDK integration, structured output parsing, SQLite persistence, and script display with beats/hooks/score.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd web && npm run dev`
    2. Open http://localhost:3000 in browser
    3. Verify: 7 format cards visible in a grid (The Bug, The Satisfaction, Before/After, The Decision, The Trick, The Fail, The Number)
    4. Select "The Bug" format (card should highlight)
    5. Type dev context: "This week I spent 3 days debugging a collision detection issue where objects would pass through walls at high speeds. Turned out I needed to switch from discrete to continuous collision detection."
    6. Click "Generate Script"
    7. Verify: loading state appears with elapsed time counter
    8. Wait for generation to complete (30-60 seconds)
    9. Verify: script displays with:
       - 2-3 hook variants (A, B, C) with visual + voiceover columns
       - 3-5 script beats with visual + voiceover columns
       - 3 title options
       - Anti-slop score with 5-dimension breakdown, total 35+/50
       - Thumbnail concept
       - Duration estimate
    10. Refresh the page -- verify the script persists (loaded from database)
    11. Click "Re-generate" -- verify form reappears with same format/context for re-generation
  </how-to-verify>
  <verify>
    <automated>cd D:/projects/content-pavyny/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms end-to-end generation flow works: format selection, dev context input, generation with loading state, structured script display, database persistence, and re-generation</done>
  <resume-signal>Type "approved" if the generation flow works end-to-end, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- All components render without React errors
- Format cards are selectable (visual highlight on click)
- Loading state shows elapsed time
- Generated script displays all sections (hooks, beats, titles, score, thumbnail)
- Script persists in SQLite across page refresh
- Re-generate button triggers new generation
</verification>

<success_criteria>
- Pavlo can select one of 7 formats, enter dev context, click Generate
- Loading state with elapsed timer appears during 30-60s generation
- Generated script displays structured beats (visual + voiceover), hook variants, titles, anti-slop score
- Script persists in database across page refresh
- Re-generate button allows producing a new version
- Clean Notion-style UI with light theme
</success_criteria>

<output>
After completion, create `.planning/phases/04-foundation-generation/04-03-SUMMARY.md`
</output>
