---
phase: 08-metrics-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - src/components/video-link-selector.tsx
  - src/app/script/[id]/page.tsx
  - src/components/editor-metrics-panel.tsx
autonomous: false
requirements: [LINK-01, LINK-02, DASH-04]

must_haves:
  truths:
    - "Pavlo can link a script to a YouTube video via a dropdown on the editor page"
    - "Pavlo can unlink a script from a video using the same dropdown"
    - "When a script is linked to a video, the editor page shows a metrics detail panel"
    - "The metrics panel shows views, retention %, subs gained, likes, comments and a retention chart"
  artifacts:
    - path: "src/components/video-link-selector.tsx"
      provides: "Dropdown for linking/unlinking videos to scripts"
    - path: "src/components/editor-metrics-panel.tsx"
      provides: "Metrics detail panel for editor page"
    - path: "src/app/script/[id]/page.tsx"
      provides: "Editor page with video linking dropdown and metrics panel"
  key_links:
    - from: "src/app/script/[id]/page.tsx"
      to: "src/app/actions/metrics.ts"
      via: "calls getVideoForScript, getUnlinkedVideos for data"
      pattern: "getVideoForScript|getUnlinkedVideos"
    - from: "src/components/video-link-selector.tsx"
      to: "src/app/actions/metrics.ts"
      via: "calls linkVideo and unlinkVideo server actions"
      pattern: "linkVideo|unlinkVideo"
    - from: "src/components/editor-metrics-panel.tsx"
      to: "src/components/retention-chart.tsx"
      via: "renders RetentionChart in expanded mode (created by 08-02)"
      pattern: "RetentionChart"
---

<objective>
Add video linking dropdown and metrics detail panel to the script editor page. Pavlo can link/unlink scripts to YouTube videos and see full metrics when linked.

Purpose: Closes the script-to-video connection loop (LINK-01, LINK-02) and shows detailed metrics in the editor context (DASH-04).
Output: Editor page has a video selector dropdown and conditionally displays a metrics panel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-metrics-dashboard/08-RESEARCH.md
@.planning/phases/08-metrics-dashboard/08-01-SUMMARY.md
@.planning/phases/08-metrics-dashboard/08-02-SUMMARY.md

<interfaces>
<!-- From Plan 08-01 outputs (src/app/actions/metrics.ts): -->
export async function linkVideo(scriptId: number, videoId: number): Promise<{ success: boolean }>;
export async function unlinkVideo(videoId: number): Promise<{ success: boolean }>;
export async function getUnlinkedVideos(): Promise<VideoData[]>;
export async function getVideoForScript(scriptId: number): Promise<{ video: VideoData; metrics: VideoMetricsData } | null>;

<!-- From src/lib/types.ts: -->
export interface VideoData {
  id: number; youtubeId: string; title: string; description: string | null;
  thumbnailUrl: string | null; publishedAt: Date | null; scriptId: number | null;
}
export interface VideoMetricsData {
  views: number; likes: number; comments: number; shares: number;
  subscribersGained: number; subscribersLost: number;
  averageViewPercentage: number; averageViewDuration: number;
  engagedViews: number; retentionCurve: number[] | null; lastSyncedAt: Date;
}

<!-- From existing script editor page (src/app/script/[id]/page.tsx): -->
- Server component querying scripts + beats by ID
- Renders Link "Back" + ScriptEditor component
- Uses max-w-3xl layout

<!-- From Plan 08-02 (retention-chart.tsx — created in Wave 2, available for this Wave 3 plan): -->
export function RetentionChart({ data, expanded }: { data: number[]; expanded?: boolean }): JSX.Element;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create video link selector and editor metrics panel components</name>
  <files>src/components/video-link-selector.tsx, src/components/editor-metrics-panel.tsx</files>
  <read_first>
    - src/app/script/[id]/page.tsx
    - src/lib/types.ts (after Plan 01 additions)
    - src/components/score-panel.tsx (existing pattern for detail panels)
    - src/components/retention-chart.tsx (created by Plan 02)
    - .planning/phases/08-metrics-dashboard/08-RESEARCH.md (Pattern 7 — Video Link Selector)
  </read_first>
  <action>
**Create src/components/video-link-selector.tsx** — Per D-12, D-13, D-14:
- "use client" component
- Props: `{ scriptId: number; linkedVideo: VideoData | null; unlinkedVideos: VideoData[] }`
- Import `linkVideo, unlinkVideo` from `@/app/actions/metrics`
- Import VideoData from `@/lib/types`
- Use a native HTML `<select>` element (matching the status dropdown pattern from scripts-table.tsx — NOT shadcn Select, to stay consistent with the project's established select pattern)
- Default option: "Link to video..." placeholder text
- When linkedVideo exists: show the linked video title as the selected value, plus an "Unlink" option at the top
- Options list per D-13: each unlinked video shows `title — date` where date is formatted as "MMM DD, YYYY" from publishedAt
- On select change:
  - If value is "unlink": call `unlinkVideo(linkedVideo.id)` per D-14
  - If value is a video ID number: call `linkVideo(scriptId, Number(value))`
- Use `useTransition` from React for pending state during server action
- While pending, disable the select
- Style: `text-sm rounded border px-2 py-1` (consistent with status dropdown)

**Create src/components/editor-metrics-panel.tsx** — Per D-04 extended for DASH-04:
- "use client" component
- Props: `{ metrics: VideoMetricsData; videoTitle: string }`
- Import Card from `@/components/ui/card`
- Import RetentionChart from `./retention-chart`
- Layout: a Card component below the back link area showing:
  - Header: video title in font-medium text-sm
  - Grid of metrics (2 columns on desktop): views, avg retention %, subs gained, likes, comments — each with label + value
  - Below grid: retention chart in expanded mode (full width, with axes) — NOT sparkline. The editor page has more room, show the full chart.
  - If retentionCurve is null or < 10 points: show "Not enough retention data yet" text
- Keep it compact but more detailed than the metrics-card.tsx (which is the inline table version).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "VideoLinkSelector" src/components/video-link-selector.tsx
    - grep -q "linkVideo" src/components/video-link-selector.tsx
    - grep -q "unlinkVideo" src/components/video-link-selector.tsx
    - grep -q "useTransition" src/components/video-link-selector.tsx
    - grep -q "EditorMetricsPanel" src/components/editor-metrics-panel.tsx
    - grep -q "RetentionChart" src/components/editor-metrics-panel.tsx
    - grep -q "VideoMetricsData" src/components/editor-metrics-panel.tsx
  </acceptance_criteria>
  <done>VideoLinkSelector renders a native select with unlinked videos (title + date), handles link/unlink via server actions with pending state. EditorMetricsPanel shows full metrics grid and expanded retention chart in a Card.</done>
</task>

<task type="auto">
  <name>Task 2: Wire video linking and metrics panel into editor page</name>
  <files>src/app/script/[id]/page.tsx</files>
  <read_first>
    - src/app/script/[id]/page.tsx
    - src/components/video-link-selector.tsx (from Task 1)
    - src/components/editor-metrics-panel.tsx (from Task 1)
    - src/app/actions/metrics.ts (from Plan 01)
  </read_first>
  <action>
Modify the script editor page to add video linking and metrics display:

1. Add imports at the top:
   - `import { getVideoForScript, getUnlinkedVideos } from "@/app/actions/metrics"`
   - `import { VideoLinkSelector } from "@/components/video-link-selector"`
   - `import { EditorMetricsPanel } from "@/components/editor-metrics-panel"`

2. After fetching script and beats, add two more queries:
   ```typescript
   const videoData = await getVideoForScript(scriptId);
   const unlinkedVideos = await getUnlinkedVideos();
   ```

3. In the JSX, between the Back link and ScriptEditor component, add:
   - VideoLinkSelector: `<VideoLinkSelector scriptId={scriptId} linkedVideo={videoData?.video ?? null} unlinkedVideos={unlinkedVideos} />`
   - Conditionally render EditorMetricsPanel when videoData exists: `{videoData && <EditorMetricsPanel metrics={videoData.metrics} videoTitle={videoData.video.title} />}`

4. Layout: Place the VideoLinkSelector on the same line as the Back link (flex row, justify-between). The EditorMetricsPanel goes below the Back/selector row, above ScriptEditor, with `mb-6` spacing.

5. Serialize Date objects to ISO strings for client components if needed (videoData.metrics.lastSyncedAt, videoData.video.publishedAt).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getVideoForScript" src/app/script/[id]/page.tsx
    - grep -q "getUnlinkedVideos" src/app/script/[id]/page.tsx
    - grep -q "VideoLinkSelector" src/app/script/[id]/page.tsx
    - grep -q "EditorMetricsPanel" src/app/script/[id]/page.tsx
  </acceptance_criteria>
  <done>Editor page queries video data and unlinked videos. VideoLinkSelector renders next to Back link. EditorMetricsPanel conditionally renders when a video is linked, showing full metrics and retention chart. TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify video linking and metrics panel on editor page</name>
  <files>src/app/script/[id]/page.tsx</files>
  <action>
Human verification of video linking and editor metrics panel. Steps:
1. Start the app: `cd web && npm run dev` (if not already running)
2. Go to http://localhost:3000/scripts and click any script to open the editor
3. Verify a "Link to video..." dropdown appears next to the Back link
4. The dropdown should list available YouTube videos (title + date) — only videos not already linked to other scripts
5. Select a video from the dropdown — the link should be created
6. After linking: a metrics panel should appear above the script editor showing views, retention %, subs gained, likes, comments, and a full retention curve chart
7. The dropdown should now show the linked video title and have an "Unlink" option
8. Click "Unlink" — the metrics panel should disappear and the dropdown returns to placeholder
9. Go back to /scripts — the linked script should now have a chevron in the Actions column (from Plan 02)
  </action>
  <verify>Human visual confirmation of all 9 verification steps</verify>
  <done>Video linking dropdown works (link and unlink), metrics panel displays correctly when linked, retention chart renders in expanded mode, changes reflect on both editor and scripts list pages.</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` compiles cleanly
- Editor page shows video link dropdown
- Linking a video shows metrics panel with retention chart
- Unlinking removes the metrics panel
- Changes revalidate both /scripts and /script/[id] pages
</verification>

<success_criteria>
- Video link dropdown on editor page shows unlinked videos with title + date (D-13)
- Selecting a video creates the link via scriptId FK (D-12)
- Unlink option available and works (D-14)
- Linked scripts show metrics detail panel with full retention chart (DASH-04)
- Server actions handle revalidation for both scripts list and editor pages
</success_criteria>

<output>
After completion, create `.planning/phases/08-metrics-dashboard/08-03-SUMMARY.md`
</output>
