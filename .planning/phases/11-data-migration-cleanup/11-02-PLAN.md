---
phase: 11-data-migration-cleanup
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified: []
autonomous: false
requirements: [CLEN-03]

must_haves:
  truths:
    - "App works identically on Windows PC and MacBook Air M1 against the same Supabase instance"
    - "Scripts created on one machine appear on the other"
    - "YouTube connection status is consistent across devices"
  artifacts: []
  key_links:
    - from: "MacBook Air M1"
      to: "Supabase PostgreSQL"
      via: "DATABASE_URL in .env.local"
      pattern: "DATABASE_URL"
---

<objective>
Verify the app works from both of Pavlo's machines against the same Supabase database.

Purpose: Confirm the entire point of the v3.0 migration — eliminating the "data stuck on one machine" problem. Both Windows PC and MacBook Air M1 must see the same data.

Output: Verified cross-device functionality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-data-migration-cleanup/11-CONTEXT.md
@.planning/phases/11-data-migration-cleanup/11-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Cross-device verification</name>
  <files>web/.env.local</files>
  <read_first>
    - .planning/phases/11-data-migration-cleanup/11-CONTEXT.md (D-03: manual verification; D-04: MacBook setup instructions)
  </read_first>
  <action>
This is a human verification checkpoint. No automated action — Pavlo performs the cross-device test manually per D-03.

Pre-check on Windows PC before handing off: run `cd web && npm run dev` and confirm the app loads with migrated data visible.
  </action>
  <what-built>
Full Supabase migration: all data transferred from SQLite to PostgreSQL, SQLite dependency removed. The app now runs entirely against the remote Supabase database.
  </what-built>
  <how-to-verify>
**On MacBook Air M1:**

1. Open the repo (already cloned per D-04)
2. Create `web/.env.local` with the same `DATABASE_URL` as on Windows PC
3. Run `cd web && npm install` — confirm NO native compilation step (no better-sqlite3)
4. Run `npm run dev` — app should start at localhost:3000
5. Verify: all existing scripts are visible in the script library
6. Verify: YouTube metrics/videos are visible (if previously synced)
7. Create a new test script on MacBook

**On Windows PC:**

8. Refresh the app in browser
9. Verify: the script created on MacBook appears in the library
10. Expected: identical data on both machines, no sync needed

**If anything fails:** describe what you see (error messages, missing data, wrong dates).
  </how-to-verify>
  <verify>
    <automated>cd D:/projects/content-pavyny/web && npm run dev -- --port 3099 &amp; sleep 3 && curl -s -o /dev/null -w "%{http_code}" http://localhost:3099 && kill %1 2>/dev/null; echo "Dev server responds"</automated>
  </verify>
  <acceptance_criteria>
    - Scripts created on MacBook appear on Windows PC (and vice versa)
    - npm install on MacBook completes without native compilation
    - Both machines show the same script library, same YouTube data
  </acceptance_criteria>
  <resume-signal>Type "approved" if both machines work identically, or describe issues</resume-signal>
  <done>App verified working from both Windows PC and MacBook Air M1 against the same Supabase instance with identical behavior</done>
</task>

</tasks>

<verification>
Human confirms both machines see the same data and can create/read scripts across devices.
</verification>

<success_criteria>
- App runs on MacBook Air M1 without errors
- npm install on MacBook has no native compilation
- Scripts visible on both machines
- New script created on one machine appears on the other
- YouTube metrics visible on both machines (if synced)
</success_criteria>

<output>
After completion, create `.planning/phases/11-data-migration-cleanup/11-02-SUMMARY.md`
</output>
